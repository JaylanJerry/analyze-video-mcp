# 本地打包产物

`.tgz` 被 git 忽略，不提交到仓库。当前 `analyze-video-mcp-1.0.0.tgz` 保留作正式发布核对；发布事实及哈希见 [发布记录](../tasks/release-1.0.0.md)。

`archive/` 保存本机旧版本包（本轮把根目录的 0.6.1 包移入），只用于历史核对，不作为当前安装入口。全新克隆未必包含这些本地产物；需要时通过 npm 获取指定公开版本或重新打包。
